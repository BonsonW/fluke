# Reference/test deps for the pure C library tests (test/*.py).
#
# The tests build lib/libfluke.so with `make` and call the C ABI via ctypes, then
# compare against a torch reference. They need a CUDA-enabled torch (NOT the +cpu
# build) plus a CUDA toolkit (CUDA_HOME, default /usr/local/cuda) to build the lib.
#
#   pip install --index-url https://download.pytorch.org/whl/cu126 -r test/requirements.txt
torch==2.7.1
